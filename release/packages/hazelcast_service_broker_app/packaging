# abort script on any command that exit with a non zero value
set -e

cp hazelcast_service_broker_app/hazelcast-cf-service-broker.war ${BOSH_INSTALL_TARGET}
cp hazelcast_service_broker_app/manifest.yml ${BOSH_INSTALL_TARGET}
